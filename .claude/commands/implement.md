# /implement

Now implement the enhancement carefully.

STRICT REQUIREMENTS:

- Do not break existing functionality
- Maintain existing theme and structure
- Mobile responsive mandatory
- Use reusable components/hooks/utils where possible
- Avoid duplicate logic
- Avoid unnecessary refactoring
- Preserve existing APIs and business logic
- Maintain TypeScript safety
- Maintain accessibility
- Keep changes isolated and incremental

Implementation Rules:

- prefer composition over rewriting
- reuse existing design system
- use utility classes where appropriate
- optimize renders where needed
- avoid large component rewrites
- maintain backward compatibility

Before modifying any file:

- explain why the file changes are required
- explain possible impact

After implementation:

- explain all changes
- mention regression-sensitive areas
- mention testing checklist
